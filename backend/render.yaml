services:
  - type: web
    name: gen-questions-backend
    env: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 3001 